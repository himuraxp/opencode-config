# Secrets — never commit
.env
**/.env
!config/.env.example
!**/.env.example

# macOS
.DS_Store
._*
Icon

# Node
node_modules/
package-lock.json
bun.lock

# Python
__pycache__/
*.py[cod]

# Feature planner: REAL GitLab project data (namespaces, project IDs) — never commit.
# Versioned template: skills/gitlab-feature-planner/projects-registry.example.json
skills/gitlab-feature-planner/projects-registry.json
**/projects-registry.json
!**/projects-registry.example.json

# IDE
.idea/
.vscode/

# Logs & tmp
*.log
*.tmp
dist/
dist-tests/

# AI session memory — local only, not for version control
# Templates for user projects are in templates/ and templates/project-docs/
docs/ai/

# Config backups (kept local, never versioned)
*.bak

# Local secret files (never commit — real secrets live outside this repo)
secrets/
